{% extends '../layouts/slate.twig' %}

{% block title %}{% set channelname = channel.getName | escape %}{{ channelname }}{% endblock %}

{% block underbar %}
    <h1>{{ channelname }}</h1>
{% endblock %}

{# TODO: Topics #}

{% block slate %}
    <h2>Users</h2>
    {% for user in channel.getUsers %} {# TODO: Use getNicknames when it's fixed #}
        {# TODO: Style this #}
        <p>
            <span class="tooltipped tooltipped-n" aria-label="{{ user.getNick | escape }}!{{ user.getUser | escape }}@{{ user.getHost | escape }}">
                {{ user.getNick | escape }}
            </span>
        </p>
    {% endfor %}
{% endblock %}
